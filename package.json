{
  "name": "snake-ts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build2": "rollup --config rollup.config.js",
    "build": "rimraf dist && rimraf out-tsc && tsc && rollup -c rollup.config.js",
    "dev": "rimraf dist && rimraf out-tsc && tsc && concurrently --kill-others \"tsc --watch\" \"rollup -w -c rollup.config.js\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@open-wc/building-rollup": "^2.0.1",
    "@rollup/plugin-typescript": "^8.3.2",
    "concurrently": "^7.2.1",
    "deepmerge": "^4.2.2",
    "rimraf": "^3.0.2",
    "rollup": "^2.75.5",
    "typescript": "^4.7.3"
  }
}
