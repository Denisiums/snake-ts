import {Snake} from '../snake';

describe('Snake', () => {
    test('Should be generated in position 10, 10 with correct segments position', () => {
        // actual test
        expect(1).toBe(1);
    });
});